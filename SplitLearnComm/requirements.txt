# SplitLearnComm - Minimal Runtime Dependencies
# This file contains the minimal dependencies required to run SplitLearnComm

# Core dependencies
torch>=2.0.0
grpcio>=1.50.0
protobuf>=4.0.0
numpy>=1.24.0

# Note: For additional features, install with:
# pip install splitlearn-comm[client]  # For client-side usage
# pip install splitlearn-comm[server]  # For server-side usage
# pip install splitlearn-comm[ui]      # For UI support (Gradio)
# Or for development: pip install -r requirements-dev.txt
